import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.einclair.awasimsak',
  appName: 'Awas Imsak',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
